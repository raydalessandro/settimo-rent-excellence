'use client';

/**
 * @deprecated Use '@/lib/features/auth' instead
 */

export { 
  useAuth, 
  useAuth as useAuthState,
  useLogin, 
  useLogout, 
  useRegister,
  useAuthActions,
} from '@/lib/features/auth';
